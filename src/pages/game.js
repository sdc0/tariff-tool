import { TariffForm } from "../components/tariff-form/tariff-form";

const Game = () => {
    return (
        <div style={{display: 'inline-flex'}}>
            <p>Route /game is working, no content</p>
        </div>
    );
}

export default Game;